export enum NeuType {
  Embossed = "embossed",
  Raised = "raised",
  Elevated = "elevated",
  Debossed = "debossed",
  Recessed = "recessed",
}

export enum NeuButtonType {
  Embossed = "embossed",
  Elevated = "elevated",
}

export enum NeuIntensity {
  Large = "lg",
  Normal = "normal",
  Small = "sm",
}
